

To get it workign on ios (the simulator part, run the following command [WARN: gotten from gemini but it worked for me])
```bash
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
```

# File Structure

`./memori-app` - Tauri code

`./memori-dev` - ESP32 code

`./doc`        - Documents for class
